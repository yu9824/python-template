name: work

channels:
  - conda-forge

dependencies:
  - python=3.12
  - jupyter
  - jupytext
  - matplotlib
  - matplotlib-fontja
  - numpy
  - pandas
  - pytest
  # for kaggle
  # - kaggle
  # - lightgbm
  # - scikit-learn
  # - networkx

  # pip パッケージは uv 経由でインストールする（例: uv pip install -e ".[dev]"）
  - pip:
      - -e .[dev,test,optional]
